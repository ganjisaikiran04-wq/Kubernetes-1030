apiVersion: v1
kind: Service
metadata:
  name: swiggy-app
  namespace: myapp
  labels:
    app: swiggy-app

spec:
  type: LoadBalancer

  selector:
    app: swiggy-app

  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
